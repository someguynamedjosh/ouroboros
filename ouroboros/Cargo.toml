[package]
name = "ouroboros"
version = "0.1.0"
authors = ["Joshua Maros <joshua-maros@github.com>"]
edition = "2018"
license = "MIT OR Apache-2.0"
description = "Easy, safe self-referential struct generation."

[dependencies]
ouroboros_macro = { version = "0.1", path = "../ouroboros_macro" }
stable_deref_trait = "1.2"
